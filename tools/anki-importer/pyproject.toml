[project]
name = "anki-csv-importer"
version = "0.1.0"
description = "Dry-run-first AnkiConnect importer for AWS certification CSV flashcards."
requires-python = ">=3.12"
dependencies = []

[tool.uv]
package = false
